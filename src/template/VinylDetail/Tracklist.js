import React from 'react'

function Tracklist({product}) {
    return (
        <>
            {/* <h4 className="mt-[30px] mb-[14px] pb-2.5 text-[20px] font-bold border-b-2 border-[#a2a9b0]">TRACKLIST</h4>
            <table className='w-full'>
                <tbody>
                    {
                        product.tracklist.map((item, index) => (
                            <tr key={index}>
                                <th className='py-[3px] capitalize min-w-[30px] text-color-primary font-light text-left'>{index + 1}</th>
                                <th className='py-[3px] capitalize min-w-[130px] text-color-primary font-light text-left w-[60%]'>{item.name}</th>
                                <th className='py-[3px] capitalize min-w-[130px] text-color-primary font-light text-left w-[40%]'>{item.duration}</th>
                            </tr>
                        ))
                    }
                </tbody>
            </table> */}
        </>
    )
}

export default Tracklist